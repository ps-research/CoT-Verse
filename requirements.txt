# Pinned to the versions used for every result in this repository.
# Full freeze: experiments/B4_multihop/env_freeze_cot3d.txt
#
# NOTE: unsloth and transformers are version-sensitive here — see
# README "Environment constraints" before upgrading either.

torch==2.10.0
transformers==5.5.0
unsloth==2026.6.9
bitsandbytes==0.49.2
accelerate
peft
datasets
huggingface-hub
hf-transfer

# Analysis and figure/table generation
matplotlib==3.11.1

# Benchmark construction (requires an API key; not needed to reproduce results)
google-genai==2.12.1
